define([], function () {
    window.requirejs.config({
        paths: {
            "mrpicker": "//cdn.jsdelivr.net/momentjs/latest/moment.min",
            "drpicker": "//cdn.jsdelivr.net/npm/daterangepicker/daterangepicker.min",
        },
        shim: {
            'moment': {exports: 'moment'},
            'daterangepicker': {exports: 'daterangepicker'},
        }
    });
});